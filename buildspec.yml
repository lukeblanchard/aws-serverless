version: 0.1

phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
      - echo Nothing to do 
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/*
    - images/*